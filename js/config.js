// STC Shield → STC AI configuration
window.STC_CONFIG = {
  // LOCAL DEV (works now)
  AI_API_BASE: "http://127.0.0.1:8000"

  // PROD (later, when deployed)
  // AI_API_BASE: "https://stc-intelligence-core.pages.dev"
};
